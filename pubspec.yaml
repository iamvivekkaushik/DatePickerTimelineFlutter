name: date_picker_timeline
description: Flutter Date Picker Library that provides a calendar as a horizontal timeline.
version: 1.4.1
homepage: https://github.com/iamvivekkaushik/DatePickerTimelineFlutter
repository: https://github.com/iamvivekkaushik/DatePickerTimelineFlutter
issue_tracker: https://github.com/iamvivekkaushik/DatePickerTimelineFlutter/issues

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  # Wide range so consumers on either intl major-minor can resolve.
  intl: '>=0.19.0 <0.21.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_lints: '>=5.0.0 <7.0.0'
  # Used by tools/publish/check_version.dart
  yaml: ^3.1.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
